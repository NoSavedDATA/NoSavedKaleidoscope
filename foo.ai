start_timer()

def int is_prime(i n)
	# print("n="+n)
	for i=2, i<(n//2+1)
		if n%i==0
			return 0
	return 1

int num_primes = 0
for i=2, i<250001
	num_primes = num_primes + is_prime(i)

print("Found " + num_primes + " primes.")

end_timer()